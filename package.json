{
  "name": "lawyer-pro-cms",
  "version": "1.0.0",
  "description": "אתר מקצועי לעורכי דין עם TinaCMS",
  "scripts": {
    "dev": "tinacms dev -c \"next dev\"",
    "build": "tinacms build && next build",
    "start": "tinacms build && next start"
  },
  "dependencies": {
    "@tinacms/cli": "^1.5.30",
    "tinacms": "^1.5.30"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
